# Sidebar header buttons landing

- Done: resumed the PR-owned closeout worktree at `10dfb706a1fc22d22df6597e79453df62c728671`; the mock/tuner worktree remains untouched and uncommitted.
- Verified: PR #134365 is open, already non-draft, and conflicts with current `origin/main` (`4e34f4621d274085a050f5822cf01d9cf1b578be`).
- Codex source gate: reviewed `codex-rs/tui/src/chatwidget/slash_dispatch.rs:267`, `codex-rs/tui/src/chatwidget/review_popups.rs:63-82`, `codex-rs/protocol/src/protocol.rs:3394-3425`, and `codex-rs/core/src/tasks/review.rs:220-240`; `/review` opens the preset picker, a base-branch choice produces `ReviewTarget::BaseBranch`, and interrupted reviews must be rerun.
- Applied: `ui/src/styles/layout.css:218-224` removes the native web-chrome new-session border/shadow; `ui/src/styles/layout.css:1020-1027` uses logical auto margin for LTR/RTL action separation; `ui/src/styles/layout.css:3198-3224` preserves the approved 1px card padding and hover/active surface; `ui/src/styles/layout.css:3322-3328` places unread attention at the avatar top-left.
- Coverage: `ui/src/e2e/sidebar-customization.e2e.test.ts:256-286` checks both LTR right-edge and RTL left-edge rail alignment; `ui/src/e2e/native-nav-sidebar-toggle.e2e.test.ts:436-458` checks the native `+` has neither visible border nor shadow.
- Rebase: replayed the five thesis commits onto `origin/main` `4e34f4621d274085a050f5822cf01d9cf1b578be`; resolved only the search-owner selector in `ui/src/e2e/custodian-panel-toggle.e2e.test.ts:144-147` and deleted stale style branches removed by current main in `ui/src/styles/layout.css:1117-1131`.
- Scope gate: production owners are `ui/src/app/app-shell-view.ts`, `ui/src/components/app-sidebar-{base,render,session-navigation*}.ts`, `ui/src/components/sidebar-agent-card.ts`, and `ui/src/styles/layout{,.mobile}.css`; all other tracked files are focused sidebar consumer/test fixtures. Tuner, canonical harness edits, screenshots, and this worklog remain outside the commit.
- LOC against current main: production `+165/-87` (net `+78`) for the moved controls, identity overflow/attention states, and host ownership; test and test-support `+123/-78` (net `+45`).
- Current head: `66f35bcef9f`; rebase merge-tree is conflict-free.
- Autoreview: interactive `/review` against `main` completed with one real P2: the absolute unread dot had no avatar containing block. The focused review run also completed 35/35 UI E2E tests before reporting.
- Repair: `ui/src/components/sidebar-agent-card.ts:70-106` now nests the unread indicator inside the already-positioned avatar; `ui/src/test-helpers/app-sidebar-cases/basics.ts:404-410` requires that ownership in the sidebar fixture. The approved top-left coordinates and all visual values remain unchanged.
- Next: commit and push the autoreview repair, inspect ClawSweeper's in-place report, then mark ready and watch exact-head CI.
- Iteration: removed the explicit horizontal SVG offsets from collapse and search in `ui/src/styles/layout.css:1075`; their 16px glyphs now sit geometrically centered in the unchanged 29px hit areas. Gap, button sizes, colors, and the new-session control are unchanged. Mirrored only this CSS delta into the live `:5279` tuner worktree for HMR review; no commit or PR update yet.
- Regression coverage: `ui/src/e2e/sidebar-customization.e2e.test.ts` now measures the collapse/search SVG centers against their hit-area centers. It returns `[2, -1]` with the removed offsets and `[0, 0]` with the correction, without coupling to icon paths or changing production seams.
- Unblocked rebase: `6abc3adf10bf17121c10cb347654e895cce261c3` is an ancestor of `origin/main` `e41d2b7e51bf4e3fa9a4b9f22f5fd1b1f0719c46`; all 12 thesis commits rebased without conflict. New pre-push head: `b9339987b05eb8d79cc06cf68d3d3a4a8e49e8bf`.
- Final autoreview: the structured Codex branch review against that exact `origin/main` completed `scoped-clean` at the required P0 threshold; no accepted/actionable finding and no scope expansion.
- ClawSweeper Rank-up move: `ui/src/styles/layout.css` now limits the legacy 48px non-chat page-header clearance to `.shell--nav-collapsed`; `ui/src/e2e/chat-side-panel-clearance.e2e.test.ts` verifies 0px with expanded navigation and 48px after collapse. This is the direct shell-chrome ownership consequence of moving the controls, not a separate design change.
- Final visual tuning: `ui/src/styles/layout.css` now uses 0px between collapse/search and a logical 5px margin before new-session, matching the approved tuner values while preserving the native host's right-edge `+`. The avatar unread dot also replaces its accent halo with a 2px `--sidebar-bg` cutout ring. Browser geometry measured 0px, 5px, and a 2px header right inset; the rebuilt isolated Control UI bundle completed successfully.
- Sidebar-toggle tooltip repair: `ui/src/app/app-shell-chrome.ts:227-237` now routes focus restoration through `ui/src/components/tooltip.ts:380-388,663-683`, which suppresses exactly the replacement control's automatic focus-open while preserving focus and later keyboard-triggered help. The native-toggle reproduction showed one unwanted tooltip before the repair and none after; `ui/src/components/tooltip.test.ts` passed 40/40 and `ui/src/e2e/native-nav-sidebar-toggle.e2e.test.ts:327-369` passed the consolidated pointer/native collapse, reopen, and explicit-keyboard flow 1/1. Mirrored only the two production files into the live `:5279` tuner; no commit or PR update in this iteration.
- Pre-land audit: live PR #134365 remains open at `fefe701e8acc7a3a715163777eb0b08d61202b5c`; current `origin/main` is `d17027cfdc5e1be1d8fcabc50aa13e6a13b83e4c`. The current-head CI failures separate into two PR-owned test issues and one main issue: `chat-side-panel-clearance.e2e.test.ts` still targeted the old semantic control owner, `sidebar-customization.e2e.test.ts` exceeded the file limit, and `src/cli/models-cli.auth-login.integration.test.ts` had an unrelated TS2790 already repaired on main by `01129d7bbe0`.
- Mechanical CI repair: `ui/src/e2e/chat-side-panel-clearance.e2e.test.ts:234,317` now drives `.sidebar-brand__collapse`; the LTR/RTL, icon-center, fade, inset, avatar-size, and plus-shadow assertions moved from the oversized customization suite into the PR-owned `ui/src/e2e/native-nav-responsive-layout.e2e.test.ts`. Max-lines ratchet, assertion-safety ratchet, targeted oxfmt, oxlint, stylelint, and `git diff --check` pass.
- Approved-default audit: the tuner source of truth is now mirrored in `ui/src/styles/layout.css`: collapse/search 0px, search/+ 5px, avatar/name 6px, header padding 0/2/10/0px, agent padding 5/6px, 28px action buttons, no new-session shadow, 28px web avatar, and 32px avatar on native macOS hosts. These are the already-approved tuner values, not a new design iteration; focused geometry coverage records them.
- Rebase preflight: a virtual candidate containing every tracked local change merges conflict-free onto current `origin/main`; final-tree diff check is clean. Candidate scope is 23 tracked files: production `+213/-100` (net `+113`), tests/support `+402/-199` (net `+203`), docs `+1/-1`. `WORKLOG.md`, the tuner worktree, and `ui/src/e2e/sidebar-header-final-proof.e2e.test.ts` remain untracked/outside publication.
- Next after explicit land authorization: rebase the tracked candidate onto current main, commit/push, regenerate and inspect final-head screenshots (including comfortable width, 240px environment+unread, native host, and guides), update the PR body, run mandatory autoreview and current ClawSweeper rank-up handling, then exact-head hosted CI and the orchestrator ready gate before merge.
- Landing complete: orchestrator approved exact head `d971d3c68b78724a477a041e22dad6709e016e09`; native review artifacts validated with zero findings, hosted CI/Testbox was accepted, and `scripts/pr merge-run 134365` landed squash commit `eb0d8830e7dc795e1bb927b5800b61a67470c8ae`.
- Post-merge: GitHub authoritatively reports `MERGED` at that commit; `.worktrees/pr-134365` was removed by the native trail, `git worktree prune` completed, the global land mutex was released, both landing markers were written, and the human notification was delivered. The tuner/bench worktree remains untouched.
