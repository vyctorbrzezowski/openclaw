# session-marquee

## Feito

- Auditoria PR #132793 head `f88b3901`: quatro bloqueios aceitos e corrigidos. (1) Reduced motion voltou ao contrato estático: ellipsis, indent 0, transition none e masks removidas; removida a exceção `!important`. (2) E2E antigo agora usa `page.mouse.move` real para entrada/saída e espera o contrato CSS `text-overflow: clip`; o cenário reduced motion exige estado estático legível. (3) todas as navegações do arquivo usam `controlUiSessionUrl`. (4) `-webkit-mask-image` acompanha `mask-image` tanto no fade quanto na limpeza.
- Test-audit: o E2E normal protege delay/cancelamento/start/snapback através do input real que estabelece `:hover`; falhava no head anterior porque `dispatchEvent` não satisfazia o guard do rAF. O E2E reduced protege a acessibilidade observável (ellipsis, indent 0, sem mask) e falhava no head anterior porque ele exigia e forçava movimento. Nenhuma seam de produção.
- Primeira execução focal: 4/6 passaram; reduced retornou o blanket computado `1e-05s` em vez do shorthand esperado e uma migração ampla de URL referiu `sessionKey` inexistente no teste de avatar. Corrigidos sem mudança de runtime.
- Execução focal final no head editado: `node scripts/run-vitest.mjs run --config test/vitest/vitest.ui-e2e.config.ts --configLoader runner ui/src/e2e/chat-flow.sidebar-presentation.e2e.test.ts` — 1 arquivo, 6/6 testes passaram em 17,58 s. `git diff --check` limpo.
- Corretiva atual reproduzida com `page.mouse.move` sobre a caixa do próprio `.hover-marquee`, sem `dispatchEvent`: `elementFromPoint` retornou o SPAN do título; quatro títulos truncados produziram um `mouseenter` real no `.session-row-host`, `:hover=true`, classe scrolling e avanço inicial do indent. Portanto listener, hit-area e seletor CSS casam na bancada estática.
- Causa diagnosticada antes de editar: `startHoverMarquee()` limpa incondicionalmente um `PendingMarquee` existente antes de criar outro. Reconexões do `ref(restartHoverMarqueeIfHovered)` durante churn do catálogo reiniciam frame+timer sob o ponteiro; se ocorrem antes dos 500 ms, a classe nunca entra e o efeito aparente é “handler não disparou”. O histórico de `ui/src/lib/hover-marquee.ts` confirma o contrato: antes da simplificação `e9dfe8a71dc`, `pendingMarquees.has(label)` era guard explícito para “preserve this label's delay; keyed label replacements get fresh state”; a simplificação removeu o guard, mas manteve o caminho de restart pelo ref.
- Owner arquitetural: `ui/src/lib/hover-marquee.ts`; a admissão do hover deve ser idempotente enquanto o mesmo label já tem frame/timer pendente. Resize real e saída já chamam `clearPendingMarquee`, portanto continuam sendo os únicos caminhos que cancelam/reiniciam legitimamente.
- Fix aplicado no owner: `startHoverMarquee()` retorna quando o mesmo label já está scrolling **ou pending**; removeu-se o `clearPendingMarquee()` incondicional do caminho de admissão. Não há debounce/timer novo.
- Regressão em `ui/src/lib/hover-marquee.test.ts`: uma segunda admissão aos 250 ms não muda o deadline original; a classe entra exatamente aos 500 ms. Test-audit: protege a idempotência observável do hover sob ref reconnect; falha no código anterior porque a segunda admissão empurra o início para 750 ms; amplia o teste proprietário sem seam de produção.
- Vídeo corretivo curto `.artifacts/control-ui-e2e/session-marquee/corrective/physical-label-hover.webm` (8,24 s): `page.mouse.move` sai de x=600 e entra no centro do próprio label em 24 passos; overlay vermelho é alimentado apenas pelos `pointermove` reais para o cursor aparecer na gravação; o script exige a chegada ao shift terminal antes de encerrar.
- Vídeo corretivo assistido via `physical-label-hover-sheet.png`: cursor entra no título, pin/menu aparecem, texto progride por quadros e termina exibindo a cauda “marquee with hover actions visible”; sem flicker/restart.
- Nova ordem do maintainer: "REPROVADO DE NOVO pelo maintainer — e agora com prova definitiva: ele rodou TEU worktree exato (copia local na maquina dele, sem tunel, velocidade nativa) e o marquee NAO funciona — hover nao desliza. Ou seja: nao e tunel, nao e HMR, nao e codigo velho — o fix esta quebrado no uso real, e o teu video passou uma prova que a realidade nao confirma. Pare de provar por video roteirizado: reproduza INTERATIVAMENTE na tua bancada (mouse real via Playwright com mouse.move gradual entrando na linha, nao dispatch de evento sintetico), descubra por que o cenario gravado diverge do hover humano — candidatos: o fix so funciona no primeiro hover; race entre hover e montagem do pin/menu; evento sintetico pulando o caminho real de pointerenter; medicao presa a estado anterior — e conserte a CAUSA. Depois prove com: (a) hover repetido 5x na mesma linha, (b) hover em linhas diferentes, (c) hover lento e rapido — video de cada um, ASSISTIDO. Enquanto o maintainer nao validar na bancada, nada de atualizar a PR 132793 alem do fix em si. Atualize o marker ao terminar."
- Restrição operacional desta iteração: nenhuma edição de corpo/comentário/mídia da PR #132793; somente o commit do fix pode ser enviado.
- Reprodução física nova usou somente `page.mouse.move` vindo de fora da linha, com passos graduais. Em cinco ciclos na mesma linha, o DOM permaneceu na geração 0 e o host ativo; no modo padrão cada ciclo iniciou a transição, descartando HMR, substituição de nó e falha exclusiva do primeiro hover.
- Divergência reproduzida com `prefers-reduced-motion`: antes do novo fix, a lógica calculava `--hover-marquee-shift: -564px` e adicionava a classe, mas `ui/src/styles/components.css` forçava `text-indent: 0`; o reset global de `ui/src/styles/base.css` também reduzia a transição a `0.01ms`. Resultado observável idêntico ao maintainer: controles aparecem e o título fica parado.
- Fix no owner CSS: a regra que pinava `text-indent: 0` foi removida; a transição de revelação, iniciada explicitamente pelo operador e única forma de alcançar a cauda cortada, isenta somente `.hover-marquee--scrolling` do reset global de duração. A medição e duração continuam pertencendo ao helper compartilhado.
- Teste E2E novo em `ui/src/e2e/chat-flow.sidebar-presentation.e2e.test.ts` usa contexto `reducedMotion: "reduce"` e `page.mouse.move(..., { steps: 12 })`, exigindo `text-indent < -1`. Test-audit: protege o resultado visível no ambiente que falhava; falha antes porque o indent computado permanece 0; nenhuma seam de produção foi criada.
- Prova interativa final gravada sob `reducedMotion: "reduce"`: `.artifacts/control-ui-e2e/session-marquee/physical-proof/same-row-five-times.webm`, `different-rows.webm` e `slow-and-fast-entry.webm`. Cada entrada aguarda/asserta que o indent alcance o shift terminal antes de seguir.
- Os três vídeos foram assistidos por contact sheets próprios; cinco ciclos, três linhas e entradas lenta/rápida mostram progressão do texto e cauda final, sem restart/flicker. Nenhum arquivo foi enviado nem a PR foi editada, conforme a ordem atual.
- Ordem de iteração do maintainer: "REPROVADO pelo maintainer — o marquee NAO funciona na bancada :6007: ao passar o mouse o titulo fica FLICKERANDO parado, sem deslizar. Sintoma classico de loop de restart: hover faz pin/overflow aparecerem, a largura muda, o marquee recalcula e reinicia infinitamente. Reproduza voce mesmo na bancada (hover em titulo longo), ache a causa raiz no dono da medicao/animacao — a medicao tem que ser estavel apos os controles montarem, sem re-trigger em cada mutacao — e conserte de verdade, sem debounce-curativo que so esconde o loop. Prova obrigatoria: VIDEO proprio (Playwright recordVideo ou captura da bancada) mostrando o cursor entrando e o titulo percorrendo ate a ultima letra, ASSISTIDO por voce antes de subir; anexe na PR 132793 substituindo a prova de hover anterior. Atualize marker ao fim. PR continua draft."
- Causa raiz confirmada em `ui/src/lib/hover-marquee.ts`: o callback do `ResizeObserver` removia `hover-marquee--scrolling` para toda notificação e chamava `startHoverMarquee()`, que voltava a chamar `observe()` no mesmo label. Reentrega de geometria idêntica/re-observe formava um ciclo de restart; não havia comparação com a largura autoritativa já medida.
- Fix em `ui/src/lib/hover-marquee.ts`: `observedMarquees` torna a inscrição idempotente por hover; `marqueeWidths` registra `clientWidth`; callbacks com largura idêntica são no-op; mudança real de largura continua reiniciando uma única vez. Nenhum debounce/timer adicional foi criado.
- Teste proprietário ampliado em `ui/src/lib/hover-marquee.test.ts`: uma mudança real 180→120 px recalcula para `-200px`; duas notificações seguintes em 120 px preservam a classe scrolling e `observe()` ocorreu uma vez. Test-audit: protege a estabilidade observável da animação; falha no head reprovado porque qualquer callback remove a classe; cobertura anterior só exercitava mudança real; nenhuma seam de produção foi criada.
- Prova em `:6007`: título de 403 px em viewport hover de 129 px teve uma única transição de classe e percorreu continuamente `text-indent` 0→`-274px`, igual ao shift, até "hover actions visible".
- Novo vídeo Playwright em `.artifacts/control-ui-e2e/session-marquee/revision/marquee-fixed.mp4`, 9,76 s, com cursor visível entrando na linha, pin/menu expostos e título chegando à última palavra. Vídeo revisado sequencialmente via contact sheet de frames e frame final; sem flicker observado.
- Commit corretivo `23d9193fb7922b31ea9d622a4a54040a23ebfabe` enviado à PR draft #132793.
- Vídeo anterior removido do corpo da PR; novo vídeo `user-attachments` (`06e06fe0-8a0e-4d5c-bde9-26c64984e214`) inserido como única prova de movimento.
- Autoreview Codex fresco do novo head contra `origin/main`: limpo, sem findings aceitos/acionáveis; nenhum código mudou depois.

- Lido `~/lanes/session-marquee-prompt.md` integralmente.
- Lido `~/lanes/PLAYBOOK.md` integralmente; fases 1-2 e invioláveis aplicáveis.
- Lidos `ui/AGENTS.md`, `scripts/AGENTS.md` e a skill `control-ui-e2e`.
- Após atualização do maintainer, relidos integralmente `.lanes/session-marquee-prompt.md` e `.lanes/PLAYBOOK.md`; `.lanes/markers/` substitui os paths antigos de marker.
- Criado worktree detached fresco de `origin/main` em `8a2f6c3c770` após `git fetch origin main`.
- Preflight de solução existente encontrou o helper compartilhado `ui/src/lib/hover-marquee.ts` e os estilos canônicos `.hover-marquee` em `ui/src/styles/components.css`, usados também pelo hovercard de atividade.
- Vinculados `node_modules` de `~/veredito-mine/repo` na raiz, em `ui/` e em cada `packages/*` disponível; `pnpm install` não foi executado.
- A bancada recebeu quatro sessões determinísticas em `scripts/control-ui-mock-dev.ts`: curta, longa, muito longa e CJK; são linhas reais da sidebar, portanto incluem os controles reais de pin e menu.
- Bancada canônica ativa em `http://127.0.0.1:6007/chat`; sem túnel e sem `open`.
- Reprodução anterior à edição: no `mouseenter`, `ui/src/lib/hover-marquee.ts:68-69` media 181 px antes de o hover reservar as ações; a largura visível depois caía para 129 px por `ui/src/styles/layout.css:2106-2116`. O deslocamento correto dependia de uma segunda notificação do `ResizeObserver`.
- Prova do reparo em navegador com `ResizeObserver` deliberadamente indisponível: largura útil 129 px, `scrollWidth` 403 px, shift `-274px`, duração `3425ms`, `text-indent` final `-274px`; pin e menu permaneceram visíveis e clicáveis.
- Inspecionadas visualmente as capturas `.artifacts/control-ui-e2e/session-marquee/after-fix-resting-fade.png` e `.artifacts/control-ui-e2e/session-marquee/after-fix-hover-marquee.png`.
- `ui/src/lib/hover-marquee.ts:70-107`: medição passou ao próximo animation frame, depois do layout de hover; cancelamento agora cobre frame e delay; `ResizeObserver` permanece dono de redimensionamentos posteriores.
- `ui/src/styles/components.css:5946-5972`: repouso usa mask gradient com `text-overflow: clip`; durante o marquee a mask sai; reduced-motion preserva o fade sem movimento.
- `ui/src/lib/hover-marquee.test.ts`: cobertura existente foi adaptada ao frame; regressão nova altera a largura entre `mouseenter` e o frame e exige o deslocamento final. Test-audit: protege a largura visível pós-hover; falha no código anterior porque ele mede sincronamente; a cobertura existente não simulava os controles mudando a largura; nenhuma seam de produção foi criada.
- `git diff --check` limpo. Nenhuma suite/build/typecheck foi executada por I.1.
- Issue #132790 criada em inglês pelo template e atribuída a `vyctorbrzezowski`.
- Provas equivalentes olhadas e enviadas via `user-attachments`: before/after em light e dark, mais vídeo MP4 do marquee longo e CJK.
- Commit `4290ba2adf3` contém somente produção + teste; fixture, provas, worklog e marker ficaram fora.
- PR draft #132793 criada, atribuída a `vyctorbrzezowski`, com `Closes #132790`, escopo nominal, consumidores e provas no corpo.
- Hard gate Codex verificado diretamente em `../codex/codex-rs/exec/src/cli.rs:26-40`, `../codex/codex-rs/exec/src/lib.rs:250-345` e `../codex/codex-rs/config/src/loader/mod.rs:516-543`: flags isolados são reconhecidos, propagados ao loader e o user config vira camada vazia.
- Autoreview Codex contra `origin/main`: limpo na primeira passagem, sem findings aceitos/acionáveis; nenhum código mudou depois.
- Watcher canônico aguardou o head exato; como a PR permanece draft por ordem do maintainer, a matriz pesada e `openclaw/ci-gate` ficaram `SKIPPED`. Checks aplicáveis concluídos com sucesso (`PR context and evidence`, `actionlint`, `no-tabs` e detectores de escopo); nenhum check falhou ou ficou pending.

## Decidido

- Nenhum `pnpm install`, suite, build ou typecheck local; a prova será a bancada mockada na porta 6007, conforme o playbook.
- Nenhum túnel/`open`, commit ou alteração fora deste worktree; marker final será criado em `.lanes/markers/` dentro dele.
- O helper compartilhado canônico foi reutilizado, não duplicado: sidebar em `ui/src/components/app-sidebar-session-row-render.ts:288-342` e catálogo em `ui/src/components/app-sidebar-session-catalog-render.ts:526-546`; hovercard em `ui/src/components/person-activity-card.ts:163-185`.

## Próximo

- Aguardar GATE 1 do Vyctor; sem commit/push até aprovação explícita.
