# Worklog — board-inset-blend

- 2026-08-30: Read the lane brief in full. Scope confirmed: remove the dashboard widget body inset, mirror PR #124883 scrollbar treatment, inspect every consumer, capture equivalent before/after screenshots, and open a draft PR without landing.
- 2026-08-30: Selected the `openclaw-pr-maintainer`, `control-ui-e2e`, and `agent-transcript` workflows for PR evidence, browser proof, and PR-body requirements.
- 2026-08-30: Applied the brief correction: PR #124883 is a technical precedent only (author: clawsweeper; no Vyctor attribution). Added full inspection of merged PR #125065, to be cited only if its board.css change is genuinely related to the frame/blend contract.
- 2026-08-30: Read the full `openclaw-pr-maintainer`, `control-ui-e2e`, and `agent-transcript` skill instructions, plus `ui/AGENTS.md` and `scripts/AGENTS.md`. The transcript workflow will not be inserted unless separately approved, and the lane's PR-copy prohibition on agent/tool references remains authoritative.
- 2026-08-30: Started three read-only evidence lanes: precedent/history, nominal consumer inventory, and deterministic visual-proof/test strategy. All Git/GitHub writes and shared-worktree edits remain lead-owned and serialized.
- 2026-08-30: `gitcrawl` was unavailable, so used the mandated `gh` fallback. Read the complete PR #124883 diff and live body/metadata: author is `clawsweeper`; its technical contract is one host frame with edge-to-edge widget content and no inset/border. Read PR #125065 diff/body: its board.css change only unifies transient menu border/shadow tokens, so it is unrelated to the widget frame/blend contract and will not be cited.
- 2026-08-30: Confirmed detached HEAD equals the local `origin/main` at `621ef9eaacec040daac334aefc935cd25112b929`; only `WORKLOG.md` is untracked. No fetch/rebase or unrelated work is present.
- 2026-08-30: Read `ui/src/styles/board.css` in full and searched all inset/body/scrollbar references. The dashboard CSS inset has a mirrored 12px sizing constant in `ui/src/lib/board/grid.ts`; removing only CSS would leave phantom auto-height, so the canonical repair must remove both plus the now-unused base token and update focused sizing proof.
- 2026-08-30: Existing-solution preflight: this is a deletion/alignment within existing CSS and grid sizing; no library, plugin, or external platform is needed. Native overflow/CSS behavior is the existing adequate solution.
- 2026-08-30: Read `test-audit` in full. The changed sizing test protects the observable owner-boundary contract (reported document height maps to rendered card height); the credible regression is retaining the removed 24px host inset in grid math. Existing `board-view-sizing.test.ts` already owns this contract, so it will be updated rather than duplicated, with no test-only production seam.
- 2026-08-30: Read the lane PLAYBOOK in full. Local validation on this host is limited to the canonical mock harness and visual capture; suites/build/typecheck stay for exact-head CI. `WORKLOG.md` and harness artifacts remain uncommitted.
- 2026-08-30: Inspected the sizing owner, board widget render path, and canonical board fixture. The fixture includes card, full-bleed, frameless, MCP App, pending/rejected, and second-tab HTML states; its `content-controls` card explicitly has zero document inset, making the host-owned 12px frame visible in the baseline.
- 2026-08-30: First mock-harness start on port 6014 failed inside the sandbox because Vite could not write its shared `.vite-temp` cache; the approved out-of-sandbox retry reached Vite but failed with host `EMFILE` while installing file watchers. No server or child process remains from either attempt.
- 2026-08-30: Independent evidence agreed on the canonical scope: remove the CSS inset/class emission, delete the unused token, remove the mirrored 12px auto-height addition, and update both existing sizing/browser contract tests. #124883 contains no `scrollbar-*` or `scrollbar-gutter` change; any scrollbar claim must instead be proven against the existing global scrollbar contract, never invented or attributed to that PR.
- 2026-08-31: Restarted the canonical mock harness on port 6014 with file-watcher polling after the host `EMFILE` limit; fixture is healthy at `/__fixtures/board/`. The first Playwright attempt exposed an invalid cross-origin `contentDocument` wait in the local capture script; replaced it with the product-owned iframe-height signal and reran successfully.
- 2026-08-31: Captured and visually inspected the baseline dashboard and focused `content-controls` card under `.artifacts/control-ui-e2e/board-inset-blend/before/`. Observed host moat: body padding `12px`, frame offsets `12px/12px`; the screenshot visibly shows the authored transparent document boxed inside the card. Existing canonical scrollbar contract resolves to `scrollbar-width: thin` and tokenized transparent-track/thumb styling, so no new `scrollbar-gutter` or scrollbar override is warranted.
- 2026-08-31: Created branch `ui/board-inset-blend` from the verified fresh detached base.
- 2026-08-31: Applied the canonical repair: removed `--widget-frame-inset`, deleted `.board-widget__body--card` and its only class emission, removed the mirrored 12px sizing constant/presentation branch, and updated the existing sizing + browser contracts to require edge-to-edge card content. Outer `card`/`full-bleed`/`frameless` presentation behavior and the themed iframe loading backdrop remain intact.
- 2026-08-31: Captured and visually inspected the equivalent post-HMR dashboard and focused card under `.artifacts/control-ui-e2e/board-inset-blend/after/`. Observed result: body padding `0px`, frame offsets `0px/0px`, card height reduced from 169px to 145px (exactly the removed 24px), and the authored document/background/scrollbar now meet the host card edge. Scrollbar color/width remained identical to baseline.
- 2026-08-31: Maintainer order superseded the original draft-PR closeout: create no PR. Delivery is now a pushed branch plus `.done` marker containing `branch=`, `sha40=`, and a summary. No PR had been created before the order.
- 2026-08-31: Audited the uncommitted diff: six intended files, production +2/-19 (net -17), tests +13/-13, `git diff --check` clean, and zero remaining references to `--widget-frame-inset` or `.board-widget__body--card`. No local test/build/typecheck suite was run, per lane PLAYBOOK; exact-head CI remains the automated validation owner.
- 2026-08-31: Mandatory structured autoreview selected the correct local diff and completed bundle/secret validation, but its Codex provider emitted heartbeats without a verdict for 40 minutes (10 minutes beyond the documented maximum). Interrupted only that exact session; result is infrastructure-incomplete, not clean. One bounded same-engine retry with an explicit timeout is next; no model/engine switch.
- 2026-08-31: The single same-engine autoreview retry hit its explicit 20-minute timeout. Terminal diagnostics showed model-catalog connection failures (`Connection failed: error sending request`); no verdict was produced. Formal autoreview remains infrastructure-incomplete and will not be retried again.
- 2026-08-31: Fresh independent diff review found one real typecheck blocker: tests passed fresh object literals with `presentation` after the lean sizing input type stopped declaring it, triggering excess-property checking. Kept the production type lean and changed the tests to construct real typed `BoardWidget` fixtures. No other blocking findings; manual fallback review judged the production owner repair canonical, with formal autoreview uncertainty retained.
- 2026-08-31: Independent re-review of the corrected diff found no remaining blocker. Excess-property issue is gone, no dead sizing surface was restored, and `git diff --check` remains clean. Final delta: production +2/-19 (net -17); tests +21/-15 (net +6).
- 2026-08-31: Scope gate — every intended diff file is justified:
  - `ui/src/styles/board.css`: removes the dashboard body inset at its only style owner.
  - `ui/src/styles/base.css`: deletes the now-unused shared inset token and stale cross-surface contract.
  - `ui/src/components/board/board-widget-cell.ts`: stops emitting the deleted card-body modifier for HTML/framed-plugin/error/grant states.
  - `ui/src/lib/board/grid.ts`: removes the mirrored 12px policy so intrinsic height no longer retains a 24px phantom gap.
  - `ui/src/components/board/board-view-sizing.test.ts`: updates the owner-boundary row/exact-height contract and presentation siblings.
  - `ui/src/components/board/board-view.browser.test.ts`: verifies the rendered default card is 300px for a 300px report with computed zero padding.
  - `WORKLOG.md` and `.artifacts/control-ui-e2e/board-inset-blend/**`: lane-only records/proof, excluded from commit.
- 2026-08-31: Created commit `8a3c4bf722512e7ac8e71282e33c6d13258652fe` (`fix(ui): remove dashboard widget frame inset`). Commit contains exactly the six scoped production/test files; `WORKLOG.md` and proof artifacts remain outside it. Next: push `ui/board-inset-blend` to the `fork` remote, create no PR, then write the `.done` marker.
- 2026-08-31: Pushed `ui/board-inset-blend` to `fork` (`vyctorbrzezowski/openclaw`) and verified the remote ref equals `8a3c4bf722512e7ac8e71282e33c6d13258652fe`. No PR was created. Next: publish the authorized lane `.done` marker with branch/SHA/summary.
- 2026-08-31: Published and reread `.lanes/markers/board-inset-blend.done`; it contains `branch=ui/board-inset-blend`, the verified 40-character SHA, and the behavior/proof/no-PR summary. Lane complete. Worktree tracks `fork/ui/board-inset-blend`; only the intentionally untracked `WORKLOG.md` remains visible to Git.
- 2026-08-31: Final nominal consumer record (kept here because the maintainer order forbids the originally planned PR body):
  - HTML/SVG Canvas widgets: default `card` iframe changes from 12px host inset to edge-to-edge; intrinsic sizing now matches authored height. `full-bleed` and `frameless` were already edge-to-edge and retain their outer-shell semantics.
  - Framed plugins/embeds (`contentKind: plugin` with `frameUrl`, including A2UI): share the iframe/body owner and now render edge-to-edge; fixed sizing remains unchanged.
  - Trusted light-DOM plugins (session progress and Workboard renderers): never received the card-body modifier, unchanged.
  - MCP Apps: use the independent `<mcp-app-view>` path and never received the card-body modifier, unchanged.
  - Pending/rejected/frame-error states for HTML/framed plugins: lose only the redundant outer inset; their own internal padding, scrolling, and controls remain.
  - Markdown: no direct Board content kind exists; rendered Markdown is HTML and follows the HTML iframe path.
  - Chat widget previews: already edge-to-edge under technical precedent #124883, unchanged by this branch.
